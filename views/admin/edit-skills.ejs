<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&family=Work+Sans:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/admin.css">
</head>
<body>
    <div class="admin-layout" style="display:flex; min-height:100vh;">
        <aside class="admin-sidebar" style="width:250px; background:#1f1f1f; color:white; padding:2rem;">
            <h2 style="margin-bottom:2rem;">Portfolio CMS</h2>
            <ul class="admin-nav" style="list-style:none; padding:0;">
                <li><a href="/admin" style="display:block; padding:0.5rem 0; color:white; text-decoration:none;">Dashboard</a></li>
                <li><a href="/admin/profile" style="display:block; padding:0.5rem 0; color:white; text-decoration:none;">Edit Profile</a></li>
                <li><a href="/admin/skills" class="active" style="display:block; padding:0.5rem 0; color:var(--accent); text-decoration:none;">Edit Skills</a></li>
                <li><a href="/admin/projects" style="display:block; padding:0.5rem 0; color:white; text-decoration:none;">Manage Projects</a></li>
                <li><a href="/" target="_blank" style="display:block; padding:0.5rem 0; color:white; text-decoration:none;">View Site</a></li>
                <li><a href="/admin/logout" style="display:block; padding:0.5rem 0; color:white; text-decoration:none;">Logout</a></li>
            </ul>
        </aside>
        
        <main class="admin-main" style="flex:1; padding:2rem;">
            <div class="admin-header" style="display:flex; justify-content:space-between; align-items:center; margin-bottom:2rem;">
                <h1 style="margin:0;">Edit Skills</h1>
                <a href="/admin" class="btn btn-secondary" style="padding:0.6rem 1.2rem; border-radius:6px; background:#ccc; color:black; text-decoration:none;">Back to Dashboard</a>
            </div>
            
            <div class="admin-content">
                <% if (success) { %>
                    <div class="success-message" style="margin-bottom:1.5rem; padding:0.8rem 1rem; background:#2ecc71; color:white; border-radius:6px;">Skills updated successfully!</div>
                <% } %>
                
                <form method="POST" action="/admin/skills" class="admin-form" style="display:flex; flex-direction:column; gap:1.5rem;">
                    <% for (let i = 0; i < 4; i++) { %>
                        <div style="background:#f8f8f8; padding:1.5rem; border-radius:10px;">
                            <h3 style="font-family:'Playfair Display', serif; margin-bottom:1rem;">Skill Category <%= i + 1 %></h3>
                            
                            <div class="form-group" style="margin-bottom:1rem;">
                                <label for="category<%= i + 1 %>" style="display:block; margin-bottom:0.5rem;">Category Name</label>
                                <input type="text" id="category<%= i + 1 %>" name="category<%= i + 1 %>" value="<%= skills[i].category %>" required style="width:100%; padding:0.6rem 1rem; border-radius:6px; border:1px solid #ccc;">
                            </div>
                            
                            <div class="form-group">
                                <label for="items<%= i + 1 %>" style="display:block; margin-bottom:0.5rem;">Skills (comma-separated)</label>
                                <textarea id="items<%= i + 1 %>" name="items<%= i + 1 %>" rows="3" required style="width:100%; padding:0.6rem 1rem; border-radius:6px; border:1px solid #ccc;"><%= skills[i].items.join(', ') %></textarea>
                                <span class="form-hint" style="font-size:0.85rem; color:#7f8c8d;">Separate each skill with a comma (e.g., React, Node.js, Python)</span>
                            </div>
                        </div>
                    <% } %>
                    
                    <button type="submit" class="btn btn-success" style="padding:0.6rem 1rem; border-radius:6px; background:var(--accent); color:white; border:none; cursor:pointer;">Save Changes</button>
                </form>
            </div>
        </main>
    </div>
</body>
</html>