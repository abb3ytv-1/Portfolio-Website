<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&family=Work+Sans:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/admin.css">
</head>
<body>
    <div class="admin-layout">
        <aside class="admin-sidebar">
            <h2>Portfolio CMS</h2>
            <ul class="admin-nav">
                <li><a href="/admin">Dashboard</a></li>
                <li><a href="/admin/profile">Edit Profile</a></li>
                <li><a href="/admin/skills" class="active">Edit Skills</a></li>
                <li><a href="/admin/projects">Manage Projects</a></li>
                <li><a href="/" target="_blank">View Site</a></li>
                <li><a href="/admin/logout">Logout</a></li>
            </ul>
        </aside>
        
        <main class="admin-main">
            <div class="admin-header">
                <h1>Edit Skills</h1>
                <a href="/admin" class="btn btn-secondary">Back to Dashboard</a>
            </div>
            
            <div class="admin-content">
                <% if (success) { %>
                    <div class="success-message">Skills updated successfully!</div>
                <% } %>
                
                <form method="POST" action="/admin/skills" class="admin-form">
                    <% for (let i = 0; i < 4; i++) { %>
                        <div style="background: var(--admin-bg); padding: 1.5rem; border-radius: 10px; margin-bottom: 1.5rem;">
                            <h3 style="font-family: 'Playfair Display', serif; margin-bottom: 1rem;">Skill Category <%= i + 1 %></h3>
                            
                            <div class="form-group">
                                <label for="category<%= i + 1 %>">Category Name</label>
                                <input type="text" id="category<%= i + 1 %>" name="category<%= i + 1 %>" value="<%= skills[i].category %>" required>
                            </div>
                            
                            <div class="form-group">
                                <label for="items<%= i + 1 %>">Skills (comma-separated)</label>
                                <textarea id="items<%= i + 1 %>" name="items<%= i + 1 %>" rows="3" required><%= skills[i].items.join(', ') %></textarea>
                                <span class="form-hint">Separate each skill with a comma (e.g., React, Node.js, Python)</span>
                            </div>
                        </div>
                    <% } %>
                    
                    <button type="submit" class="btn btn-success">Save Changes</button>
                </form>
            </div>
        </main>
    </div>
</body>
</html>
