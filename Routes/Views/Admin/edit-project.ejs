<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&family=Work+Sans:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/admin.css">
</head>
<body>
    <div class="admin-layout">
        <aside class="admin-sidebar">
            <h2>Portfolio CMS</h2>
            <ul class="admin-nav">
                <li><a href="/admin">Dashboard</a></li>
                <li><a href="/admin/profile">Edit Profile</a></li>
                <li><a href="/admin/skills">Edit Skills</a></li>
                <li><a href="/admin/projects" class="active">Manage Projects</a></li>
                <li><a href="/" target="_blank">View Site</a></li>
                <li><a href="/admin/logout">Logout</a></li>
            </ul>
        </aside>
        
        <main class="admin-main">
            <div class="admin-header">
                <h1><%= project ? 'Edit Project' : 'Add New Project' %></h1>
                <a href="/admin/projects" class="btn btn-secondary">Back to Projects</a>
            </div>
            
            <div class="admin-content">
                <% if (success) { %>
                    <div class="success-message">Project saved successfully!</div>
                <% } %>
                
                <form method="POST" action="/admin/projects/save" class="admin-form">
                    <% if (project) { %>
                        <input type="hidden" name="id" value="<%= project.id %>">
                    <% } %>
                    
                    <div class="form-group">
                        <label for="title">Project Title</label>
                        <input type="text" id="title" name="title" value="<%= project ? project.title : '' %>" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="type">Project Type</label>
                        <input type="text" id="type" name="type" value="<%= project ? project.type : '' %>" required>
                        <span class="form-hint">e.g., Web Application, Mobile App, UI/UX Design</span>
                    </div>
                    
                    <div class="form-group">
                        <label for="description">Description</label>
                        <textarea id="description" name="description" required><%= project ? project.description : '' %></textarea>
                    </div>
                    
                    <div class="form-group">
                        <label for="technologies">Technologies (comma-separated)</label>
                        <input type="text" id="technologies" name="technologies" value="<%= project ? project.technologies.join(', ') : '' %>" required>
                        <span class="form-hint">e.g., React, Node.js, MongoDB</span>
                    </div>
                    
                    <div class="form-group">
                        <label for="link">Project Link (optional)</label>
                        <input type="url" id="link" name="link" value="<%= project ? project.link : '' %>">
                        <span class="form-hint">URL to live project or repository</span>
                    </div>
                    
                    <div class="form-group">
                        <label for="image">Image URL (optional)</label>
                        <input type="url" id="image" name="image" value="<%= project ? project.image : '' %>">
                        <span class="form-hint">URL to project screenshot or image</span>
                    </div>
                    
                    <button type="submit" class="btn btn-success">Save Project</button>
                </form>
            </div>
        </main>
    </div>
</body>
</html>
